{% extends 'default/base.html.twig' %}

{% block content %}
<div class="container-fluid">

<div class="table-responsive">
  <table class="table">
    <thead>
        <tr>
          <th>Rut</th>
          <th>Nombre</th>
          <th>Contrato Marco</th>
          <th>Acción</th>
        </tr>

    </thead>



  <br />

  {% for proveedor in proveedores %}
      <tr>
      <td>{{ proveedor.rut }}</td>
      <td>{{ proveedor.nombre }}</td>
      <td>{{ proveedor.contrato }}</td>
      <td><a class="btn btn-success" href="{{ path('proveedorEdit',{'id': proveedor.id })  }}">Editar</a>
       <a class="btn btn-danger" href="{{ path('proveedorDelete',{'id': proveedor.id }) }}">Eliminar</a></td>
      </tr>
  {% endfor %}
  </table>
</div>
</div>

{% endblock content %}
